using UnityEngine;

public class StoryHandler : MonoBehaviour
{
    [Header("Story Settings")]
    [SerializeField]
    private bool IsStoryModeEnabled;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
